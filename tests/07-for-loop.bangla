# পরীক্ষা ০৭: for loop - জন্য

জন্য (ধরি i = ১; i <= ৫; i = i + ২) {
    লিখ i;
}

# counting down
জন্য (ধরি j = ৩; j >= ১; j = j - ১) {
    লিখ j;
}

# nested for loops
জন্য (ধরি a = ১; a <= ৩; a = a + ১) {
    জন্য (ধরি b = ১; b <= ২; b = b + ১) {
        লিখ a * ১০ + b;
    }
}
